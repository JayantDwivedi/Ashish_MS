import { Box } from '@mui/material';
import Heading from 'components/common/Heading';

const Dashboard = () => (
	<main>
		<Box marginY={2}>
			<Heading heading='Welcome to the Dashboard' />
		</Box>
		<Box marginY={3} />
	</main>
);

export default Dashboard;
